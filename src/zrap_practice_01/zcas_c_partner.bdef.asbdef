projection;
strict ( 1 );
use draft;

define behavior for ZCAS_C_PARTNER    alias Partner //alias <alias_name>
{
  use create;
  use update;
  use delete;
    use action Edit;
  use action Activate;
  use action Discard;
  use action Prepare;
  use action Resume;
  use action fillEmptyStreets;
  use action clearAllEmptyStreets;
  use action copyLine;
  use action withPopup;
  use action FillEmptyStreetInstanceFeature;
  use action clearEmptyStreet;



}